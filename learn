#include <iostream>
using namespace std;

float temperature(int value){
  return 1.8 * value +32;
}
int main() {
    int celsius;
    float result;
    cout <<"輸入攝氏溫度:" ;
    cin >> celsius;
    result = temperature(celsius);
    cout << "    華氏溫度:"<< result;
  }
